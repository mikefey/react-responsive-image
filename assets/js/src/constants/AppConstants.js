/**
 * Constants
 * @object
 */

const config = {};

const actions = {
  WINDOW_RESIZE: 'WINDOW_RESIZE',
  SET_LIGHT_UI: 'SET_LIGHT_UI',
};

const constants = Object.assign(config, actions);

export default constants;
